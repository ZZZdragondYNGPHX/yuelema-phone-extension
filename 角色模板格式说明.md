# 约了吗角色模板格式说明

适用格式：`yuelema.character/v1`

## 一、输入形式

- 必须是一个合法的 JSON 对象，不能粘贴 Markdown 代码围栏或说明文字。
- JSON 的结构符号必须使用英文半角符号：`{ } [ ] : , "`。
- 字段名支持三种等价写法：`"昵称"`、`"昵称:"`、`"昵称："`。这项兼容适用于所有层级。
- 同一个对象中不能同时出现标准字段名和带冒号字段名，例如不能同时出现 `"昵称"` 与 `"昵称："`。
- 字段值可以正常使用中文或英文标点；不能包含控制字符或可执行 HTML。
- 不允许任何未列出的额外字段，也不允许 API Key、令牌、密码、连接预设、UID 等敏感或运行时字段。

## 二、完整示例

```json
{
  "format": "yuelema.character/v1",
  "character": {
    "成人验证": true,
    "公开资料": {
      "昵称": "林澈",
      "头像引用": "",
      "年龄段": "25-29",
      "性别": "女",
      "性取向": "双性恋",
      "城市": "上海",
      "距离范围": "12 km",
      "寻找意图": "先聊天，再认真约会",
      "简介": "周末看展，也喜欢深夜散步。",
      "兴趣标签": ["电影", "夜跑"],
      "生活方式标签": ["夜猫子"],
      "性格标签": ["直接"],
      "沟通风格标签": ["慢热"]
    },
    "仅好友资料": {
      "关系状态": "单身",
      "边界与偏好": "先确认边界，尊重拒绝。"
    },
    "隐藏资料": {
      "实际年龄": 28,
      "私人备注": "对临时失约比较敏感。"
    },
    "绘图": {
      "core_dna": "adult woman, short black bob, warm brown eyes",
      "outfit_dna": "cream knit cardigan, dark denim skirt"
    },
    "偏好与边界": "偏好坦诚交流，不接受骚扰或胁迫。",
    "拒绝阈值": 35,
    "已读不回阈值": 55,
    "取消匹配阈值": 75,
    "拉黑阈值": 90,
    "与玩家关系": {
      "状态": "陌生",
      "全局账号表现": 68,
      "NPC专属匹配度": 72,
      "好感": 0,
      "信任": 0,
      "戒备": 20,
      "面基意愿": 0,
      "友情值": 0,
      "心动值": 0,
      "欲望值": 0
    }
  },
  "avatar": {
    "kind": "placeholder"
  }
}
```

## 三、字段要求

### 顶层

- `format`：必填，必须等于 `yuelema.character/v1`。
- `character`：必填，结构见下文。
- `avatar`：可选。建议不手写图片数据，先使用 `{ "kind": "placeholder" }`。

### character

- `成人验证`：必须是布尔值 `true`，不能写成字符串 `"true"`。
- `公开资料`、`仅好友资料`、`隐藏资料`、`偏好与边界`、四个阈值和`与玩家关系`：必填。
- `绘图`：可省略；省略后系统会补为空的绘图 DNA。

### 公开资料

- 必填字符串：`昵称`、`年龄段`、`性别`、`性取向`、`城市`、`距离范围`、`寻找意图`、`简介`。
- `头像引用`：必填字符串，可写空字符串 `""`。
- 标签数组：`兴趣标签`、`生活方式标签`、`性格标签`、`沟通风格标签`。每项必须是字符串，单个数组最多 24 项，单项最多 32 个字符。
- `年龄段`必须明确为成年人，不能包含小于 18 的年龄。
- 公开资料不得包含未成年人、非自愿、胁迫、犯罪、具体联系方式、住址、证件或其他私密身份信息。

### 仅好友资料与隐藏资料

- `仅好友资料.关系状态`：非空字符串。
- `仅好友资料.边界与偏好`：非空字符串。
- `隐藏资料.实际年龄`：18–120 的整数。
- `隐藏资料.私人备注`：字符串，可以为空。

### 绘图

- `core_dna`、`outfit_dna`：字符串，可以为空；用于英文绘图标签。

### 阈值与关系

- `拒绝阈值`、`已读不回阈值`、`取消匹配阈值`、`拉黑阈值`：均为 0–100 的整数。
- `与玩家关系.状态`：新角色必须为 `"陌生"`。
- 其余关系数值均为 0–100 的整数。
- 新角色的 `友情值`、`心动值`、`欲望值`必须全部为 `0`。

### 头像

- 占位头像：`{ "kind": "placeholder" }`。
- 内嵌头像：`{ "kind": "embedded", "dataUrl": "data:image/..." }`，只接受经过文件签名校验的 PNG、JPEG 或 WebP Base64 Data URL。
- 不接受 `url`、`blob`、本地文件路径、HTML 或其他头像字段。普通用户建议进入创建页后再选择本地图片。

## 四、常见错误

- 用中文全角 JSON 结构符号，例如 `｛`、`｝`、`，`、`“”`：JSON 无法解析。
- 把数字或布尔值写成字符串，例如 `"实际年龄": "28"`、`"成人验证": "true"`。
- 少字段、多字段、改了字段名，或把字段放错层级。
- 标签写成逗号分隔字符串；JSON 模板中必须写成数组，例如 `["电影", "夜跑"]`。
- 关系状态不是 `陌生`，或三条新关系路线不是 `0`。
- 模板中包含 API Key、连接设置、UID、未成年人信息或不允许的头像来源。
